import React from 'react';
import ReactDom from 'react-dom';

const Home = () => {
    return (
        <div>
            <h1>TV Show Quote Generator</h1>
            <p>Click on "Breaking Bad" or "Parks and Rec" in the navigation bar to generate a quote from that show.</p>
        </div>
    )
};

export default Home;