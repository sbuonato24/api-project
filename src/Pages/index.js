import React from 'react';
import ReactDom from 'react-dom';

const home = () => {
    return (
        <div>
            <h1>
                home page
            </h1>
        </div>
    );
};

export default home;